# Secrets — never commit API keys
.env
.env.*
*.key

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/mcp.json

# deps / local test artifacts (package-lock.json IS committed for reproducible npm ci)
node_modules/
test.mjs
